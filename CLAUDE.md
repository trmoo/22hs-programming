# CLAUDE.md — 고등학교 「프로그래밍」 웹 교과서

이 파일은 이 저장소에서 작업하는 모든 사람과 에이전트가 먼저 읽는 규칙 문서다.
설계 결정은 [docs/PLAN.md](docs/PLAN.md), 참고 사이트 분석은 [docs/reference-analysis.md](docs/reference-analysis.md)에 있다.

---

## 1. 프로젝트 목적

2022 개정 교육과정 고등학교 「프로그래밍」 과목을 **웹 교과서**로 만든다.

| 목표 | 의미 |
|---|---|
| 성취기준이 뼈대 | 단원·차시 체계는 교육과정 성취기준에서만 도출한다. 모든 차시는 성취기준 코드에 **1:1 이상**으로 매핑된다 |
| 어느 교과서를 쓰든 찾아온다 | 출판사 검정 교과서는 **대조표와 라벨 병기**로만 다룬다. 목차를 출판사에 맞추지 않는다 |
| 본문은 우리가 쓴다 | 설명·예제·문항을 성취기준과 해설을 근거로 새로 집필한다 |
| 언어 두 갈래 | 페이지 상단에서 Python↔C를 전환한다. **Python은 브라우저에서 실행**(Pyodide), **C는 확인된 기대 출력을 함께 싣는다**(3-5 참고) |
| 학교에서 실제로 돌아간다 | 학교 전산실 크롬이 기준이다. 오프라인·저사양·프록시 환경에서도 **본문 학습은 깨지지 않는다** |

대상 독자는 고등학생이며, 교사가 수업 자료로 함께 쓴다.

### 이 과목의 성격 — 반드시 인지할 것

**「프로그래밍」은 보통 교과가 아니다. NCS 기반 전문 교과 기초 과목이다.**

씨마스 교과서 머리말(PDF 2쪽)에 명시되어 있다.

> "프로그래밍 교과는 NCS(국가 직무 능력 표준) 교육과정의 기초 과목으로 공업 계열 및 상업 계열의 특성화고등학교, 마이스터고등학교에서 선택할 수 있으며 …"

그래서 [별책 4] 고등학교 교육과정 「정보과」(정보 / 인공지능 기초 / 데이터 과학 / 소프트웨어와 생활, 성취기준 80개)에 **없다.** `D:\project_AI\01. 교육과정\고등학교교육과정_성취기준.md`를 아무리 찾아도 나오지 않는다. 같은 문서 부록 B가 「정보과학」에 대해 밝힌 것과 같은 상황이다.

이 사실이 프로젝트에 주는 영향:

| 항목 | 상태 |
|---|---|
| 성취기준 **본문** | 교과서에서 복원 가능. **성림출판(Python)이 중단원마다 "성취기준" 레이블과 함께 원문을 인쇄**하고, 중단원 마무리에 "성취기준 스스로 평가하기" 표(성취기준 + 되돌아가기 쪽수)를 둔다 |
| 성취기준 **코드** | **확보(2026-08-07).** `프그01-01` ~ `프그03-04` 14개. 근거는 「고등학교 프로그래밍(파이썬) 교수학습 및 평가 운영 계획」(일반고용) 2~8쪽. 교과서 5종에는 코드 표기가 없다 |
| 영역(대단원) 체계 | 교과서 6종 교차 일치 + 평가계획서의 단원명 열(Ⅰ 프로그래밍 개요 / Ⅱ 프로그래밍 기초 / Ⅲ 프로그램 설계와 구현). 다만 그 열은 교과서 단원명일 수 있어 고시 원문 확인이 남아 있다 |
| 확정 경로 | NCS 기반 전문 교과 교육과정 고시 별책 (공업 계열 / 상업 정보 계열 기초 과목) |

→ 코드와 본문은 확보했다(3-3). **남은 미확인은 영역명과 고시 원문 표기 형식뿐이며, 추측해서 채우지 않는다.**

### 교과서 PDF 상태

| 교과서 | 텍스트 추출 | 비고 |
|---|---|---|
| 성림출판(Python) | ✅ 241쪽 | **성취기준 본문 명시** — 복원의 1차 근거 |
| 씨마스커뮤니케이션(Python) | ✅ 260쪽 | |
| 삼양미디어(Python) | ✅ 255쪽 | 앞부속 + 3개 대단원 파일로 분리 |
| 삼양미디어(C) | ✅ 272쪽 | |
| 씨마스(C) | ✅ 304쪽 | 과목 성격 근거(머리말) |
| **웅보출판사(C)** | ❌ **0자** | 순수 스캔 이미지 PDF. OCR 없이는 데이터 없음 → `verified: false` 유지 |

추출 방법(재현용): PyMuPDF가 설치되어 있다. `python -c "import fitz; ..."`로 페이지별 텍스트를 뽑을 수 있다. `pdftotext`도 있다. `pdftoppm`은 없어서 Read 도구의 PDF 렌더링은 쓸 수 없다.

---

## 2. 디렉터리 구조

```
22개정 프로그래밍/
├─ CLAUDE.md                        ← 이 파일
├─ package.json                     검증·빌드 스크립트
├─ .gitignore
│
├─ docs/
│  ├─ PLAN.md                       사이트맵·데이터 모델·Phase 1~5·결정 항목
│  └─ reference-analysis.md          참고 사이트(wonedu.org) 구조 분석
│
├─ schemas/
│  └─ curriculum.schema.json        교육과정 데이터 JSON Schema (단일 진실 공급원의 규격)
│
├─ content/
│  ├─ curriculum.json               ★ 실제 프로젝트 데이터 (성취기준·단원·차시·출판사 매핑)
│  ├─ curriculum.example.json       스키마 사용 예시 — 「인공지능 기초」 실데이터. 배포물 아님
│  └─ units/{NN}/{NN}.mdx           차시 본문 (38차시 전부 집필 완료 — status: review)
│
├─ tools/                           검증 스크립트 (전부 의존성 없음)
│  ├─ validate-curriculum.mjs       교육과정 데이터 검증
│  ├─ check-content.mjs             ★ 차시 본문 ↔ curriculum.json 이중 확인
│  ├─ check-examples.mjs            예제 코드를 실제로 실행해 기대 출력 대조
│  ├─ test-toc.mjs                  목차 강조 판정 단위 테스트
│  └─ check-html.mjs                빌드 산출물 전수 검사
│
├─ astro.config.mjs                 base 경로·빌드 형식
├─ tsconfig.json                    경로 별칭 (@lib, @components, @layouts)
│
├─ src/
│  ├─ lib/
│  │  ├─ curriculum.ts              ★ curriculum.json 접근 계층. 모든 화면이 여기서 파생된다
│  │  ├─ url.ts                     내부 링크 생성 (base 경로 대응)
│  │  └─ toc.ts                     목차 강조 판정 (순수 함수, 테스트 대상)
│  ├─ layouts/BaseLayout.astro       3단 레이아웃 셸
│  ├─ components/                    TopBar · UnitTree · TocRail · StandardBadge
│  ├─ pages/
│  │  ├─ index.astro                 과목 홈
│  │  ├─ units/[unit]/index.astro    단원 열기
│  │  ├─ units/[unit]/[lesson].astro 차시 (38개 생성)
│  │  ├─ standards/index.astro       성취기준 색인
│  │  └─ crosswalk/index.astro       교과서 대조표
│  └─ styles/                        tokens.css · global.css · components.css
│
├─ public/                          favicon.svg · fonts/ (자체 호스팅)
├─ dist/                            빌드 산출물 (git 제외)
│
└─ 교과서 PDF (기존 파일 — 참고용, 저장소·배포물에 넣지 않는다)
   ├─ 프로그래밍(C)(삼양미디어).pdf
   ├─ 프로그래밍(C)(씨마스).pdf
   ├─ 프로그래밍(C)(웅보출판사).pdf
   ├─ 프로그래밍(Python)(성림출판).pdf
   ├─ 프로그래밍(Python)(씨마스커뮤니케이션).pdf
   └─ 삼양미디어(Python)(삼양교육)/교과서 PDF/
```

### 파일 배치 규칙

- **구조·매핑·대조표는 `content/curriculum.json`**, 본문은 `content/units/`. 둘을 섞지 않는다.
- 성취기준 코드는 데이터에만 존재한다. HTML/Markdown에 코드를 하드코딩하지 않고 데이터에서 렌더링한다.
- URL과 폴더명은 **번호**를 쓴다(`units/01/03/`). 한글 폴더명은 쓰지 않는다 — 퍼센트 인코딩 문제.
- 임시 파일·실험은 저장소에 두지 않는다.

---

## 3. 콘텐츠 작성 규칙

### 3-1. 성취기준 매핑은 필수

- 모든 차시는 `담당성취기준`에 **최소 1개** 코드를 갖는다. 빈 배열은 오류다.
- 차시가 담당한다고 적은 성취기준은 **본문에서 실제로 다뤄야 한다.** 배지만 붙이고 내용이 없으면 매핑이 아니다.
- 성취기준 하나를 여러 차시가 나눠 담당하는 것은 허용한다(n:1). 반대로 **어느 차시도 담당하지 않는 성취기준이 남으면 커버리지 구멍**이며 검증에서 경고가 뜬다.
- 확인 문제·평가 문항도 성취기준에 매핑한다.
- 성취기준 **원문은 그대로 인용한다.** 교육과정 고시는 공공 문서이고, 문구와 코드가 정확해야 교사가 신뢰한다. 요약·의역해서 원문 필드에 넣지 않는다(요약이 필요하면 별도 필드).

> 전재 금지 대상은 **출판사 교과서**다. 교육과정 고시 원문과 혼동하지 않는다.

### 3-2. 교과서 원문 전재 금지

- 출판사 교과서의 **본문 문장, 삽화·사진·도표, 평가문항, 예제 코드**를 옮기지 않는다. 문장을 조금 바꾸는 것도 전재다.
- 교과서에서 가져올 수 있는 것은 **대조표에 필요한 서지 정보뿐**이다 — 단원명, 절 제목, 쪽수.
- 예제 코드는 새로 작성하고, 실제로 실행해 출력을 확인한 것만 싣는다.
- 삽화는 자체 제작(SVG/CSS 도식)을 원칙으로 한다. 사진이 필요하면 CC 라이선스 이미지를 쓰고 **출처와 라이선스를 캡션에 명기**한다.
- 문항은 자체 출제한다. 데이터의 `출처` 필드는 항상 `self`다.

### 3-3. 성취기준 코드는 `프그NN-NN` 이다 (2026-08-07 확보)

접두어는 **`프그`**, 코드는 **`프그01-01` ~ `프그03-04`** 14개다. 학년군 접두(`12`)가 붙지 않는다.

**근거**: 「고등학교 프로그래밍(파이썬) 교수학습 및 평가 운영 계획」(2022 개정, 일반고용) — 교육부 <2024 초·중등학교 정보공시 지침 개정>에 따른 예시 자료. 2~8쪽 "교육과정 성취기준" 열에 14개가 전부 `[프그 NN-NN]` 형태로 인쇄되어 있다. 검정 교과서 6종에서는 코드 표기를 하나도 찾지 못했다(정규식 전수 검색 0건).

- **`12프로01-01` 같은 표기를 만들지 않는다.** 근거가 없고 고시 코드로 오인된다.
- 본문 원문은 **두 독립 출처가 일치**한다 — 평가계획서(코드·본문)와 성림출판 교과서(본문). 14개 중 11개가 글자 단위로 같고, 3개는 문장부호만 달라 평가계획서 쪽을 따랐다.
- **아직 남은 것**: 이 문서는 고시 별책 자체가 아니라 그것을 인용한 학교용 예시 자료다. 고시 원문을 확보하면 표기 형식(대괄호 안 공백, 학년 접두 유무)을 한 번 더 대조한다. 코드 자체가 바뀔 가능성은 낮다.
- 이전에 쓰던 `잠정NN-NN` 형식은 전부 교체했다. 코드를 다시 바꾸게 되면 `npm run check:curriculum`과 `npm run check:content`로 참조 무결성을 확인한다 — 차시의 `담당성취기준`, 본문 프런트매터의 `standards`, 문항의 `std` 세 곳이 함께 움직인다.

### 3-4. 확인 안 되면 `[확인필요]`

- 근거를 대지 못하는 단원명·차시 구성·성취기준 문구·쪽수는 **정확히 `[확인필요]`** 라고 적는다. 다른 표기(`TODO`, `???`, 빈 문자열)를 쓰지 않는다 — 검증 스크립트가 이 문자열을 센다.
- 확인한 항목은 출처와 확인일을 함께 남긴다(`출처.문서`, `출처.확인일`, `확인: true`).
- **`상태: "published"`인 항목에 `[확인필요]`가 남아 있으면 오류다.** 배포 전에 반드시 0이 되어야 한다.
- 추정을 적어야 할 때는 `[확인필요]`와 함께 근거 수준을 밝힌다.
  좋음: `"단원명": "[확인필요]"` + 주석에 "삼양미디어(Python) 교과서는 3단원 체계이나 교육과정 영역과 같다는 근거 없음"
  나쁨: `"단원명": "프로그래밍의 개요"` (출판사 편집 결과를 교육과정으로 위장)

### 3-5. 본문 파일과 쓸 수 있는 컴포넌트

본문은 `content/units/{단원2자리}/{차시2자리}.mdx` 한 파일이다. 프런트매터는 `curriculum.json`과 **이중으로** 적는다 — 두 곳이 어긋나면 검사에서 걸린다.

```yaml
---
unit: 1                      # curriculum.json 의 단원번호
lesson: 1                    # curriculum.json 의 차시번호
title: 프로그램과 프로그래밍 언어  # curriculum.json 의 차시명과 같아야 한다
standards: ['프그01-01']      # curriculum.json 의 담당성취기준과 같아야 한다
lead: 한 문단짜리 도입 문장
status: draft                # draft | review | published
langs: ['python', 'c']
todo: []                     # 남은 확인 사항. 비어 있어야 published 가 된다
---
```

**컴포넌트는 import 하지 않는다.** 아래 이름을 그대로 쓰면 된다(레이아웃이 주입한다).

| 컴포넌트 | 용도 | 필수 속성 |
|---|---|---|
| `<InfoBox variant="…">` | 강조 박스. `think`·`tip`·`info`·`warn`·`story` | — |
| `<CodeSample lang expect>` | 예제 코드 + **기대 출력**. 코드는 `{\`…\`}` 안에 | `lang`, `expect` |
| `<CheckQuiz>` + `<QuizItem>` | 확인 문제. 클릭 즉시 채점·해설 | `stem`, `choices`, `answer`, `explain` |
| `<Term ko en desc>` | 전문용어 원어 병기 + 설명 말풍선 | `ko` |
| `<MiniCards items>` | 개념 2~4갈래 분할 카드 | `items` |
| `<CompareTable headers rows>` | Python↔C 대조 표 | `headers`, `rows` |
| `<Activity kind title>` | 활동·실습 | `kind`, `title` |
| `<MoreBox tag title>` | 더 알아보기 접기 블록 | `title` |
| `<Figure caption>` | 그림. 사진은 `source` 필수, 자체 도식은 `self` | `caption` |
| `<LessonLink unit lesson>` | 다른 차시로 가는 링크. 차시명은 데이터에서 온다 | `unit`, `lesson` |
| `<CodeTask lang title>` | **학생이 직접 타이핑하는 코드 실습.** 붙여넣기가 막힌다 | `lang`, `title` |

**코드를 직접 쳐 보게 하는 실습에는 `<CodeTask>`를 쓴다.** 입력 칸에서 `paste`·`drop`·`copy`·`cut`·우클릭을 막아 예제를 복사해 붙이지 못하게 한다. 예제를 그대로 옮기면 괄호·따옴표·쉼표를 어디에 두는지가 손에 남지 않기 때문이다.

**다만 완전 차단이 아니다.** 개발자 도구, 브라우저 확장, 다른 창을 거치면 우회된다. 손이 가는 복붙을 기본 행동에서 빼는 수준이며, **부정행위 방지 장치가 아니라 학습 장치**다. 평가에 쓸 수 있는 통제라고 설명하지 않는다.

실습 옆에 두는 예제에서 복사 단추까지 없애려면 `<CodeSample nocopy>`를 쓴다. **C 예제에는 함부로 켜지 않는다** — C는 브라우저에서 실행하지 않으므로 복사해 전산실 컴파일러로 가져가는 것이 정해진 경로다(PLAN.md 결정 항목 6).

**본문에 내부 링크를 절대 경로로 직접 적지 않는다.** `[3차시](/units/01/03/)` 처럼 적으면 `BASE_PATH`를 바꿔 하위 경로에 배포할 때 전부 깨진다. 차시 링크는 `<LessonLink>`를 쓴다 — `href()`를 거치고 차시명도 `curriculum.json`에서 가져오므로 제목을 두 곳에 적지 않는다.

**`<CodeSample>`의 `expect`는 실제로 실행해 얻은 출력이어야 한다.** `npm run check:examples`가 코드를 뽑아 직접 실행하고 대조한다 — Python은 `python`, C는 `zig cc`로 컴파일해 돌린다. 지어낸 출력은 이 검사에서 걸린다.

### C와 Python의 실행 방식이 다르다

| | 브라우저 실행 | 검증 방법 |
|---|---|---|
| Python | ✅ Pyodide (Phase 4) | `python`으로 실행해 `expect` 대조 |
| **C** | ❌ **하지 않는다** | `zig cc`로 컴파일·실행해 `expect` 대조 |

C를 브라우저에서 실행하지 않는 이유는 PLAN.md 결정 항목 6번에 있다. 요약하면 경량 인터프리터(JSCPP)는 **한글을 출력하지 못하고**, wasm 컴파일러는 학교망에서 감당 못 할 용량이다.

그래서 C 예제는 **확인된 기대 출력을 함께 싣고**, 학생은 코드 복사 버튼으로 가져가 전산실 컴파일러에서 돌린다. C 블록 머리에 "확인된 출력" 배지가 자동으로 붙는다 — `norun`을 따로 적을 필요가 없다.

`norun="이유"`는 예외적인 경우(예: 무한 루프를 일부러 보여 주는 예제)에만 쓴다.

### 3-6. 차시 본문의 표준 구성

순서를 지킨다. 참고 사이트 분석에서 검증된 리듬이다.

1. 히어로 — 단원·차시 라벨 → 제목 → 도입 문장 → **성취기준 배지** → 핵심 개념 칩
2. 학습 목표 — 2~3개
3. 생각 열기 — 상황·문제 제시와 발문
4. 개념 전개 — 번호 붙은 섹션. 개념 → 예제 코드 → 실행 → 해석
5. 활동/실습 — 코드 수정 과제, 비교 실험
6. 개념 확인 문제 — 정답·해설 토글
7. 더 알아보기 — 접기 블록
8. 이전/다음 차시

---

## 4. 문체 규칙

### 4-1. 기본 문체

- **평서문, `-다` 종결을 기본으로 한다.** 해요체(`~해요`, `~이에요`)와 구어체 감탄(`~죠`, `~랍니다`)을 쓰지 않는다.
  - ○ `변수는 값을 담아 두는 이름이다.`
  - ✕ `변수는 값을 담아 두는 이름이에요.`
- 발문은 필요한 곳(생각 열기, 활동 지시)에만 쓴다. 본문 설명을 질문으로 늘리지 않는다.
- 지시문·활동 안내는 `~해 보자` / `~하시오`가 아니라 **`~한다`, `~해 보자` 중 하나로 파일 전체에서 통일**한다. 기본은 `~해 보자`.
- 독자를 `여러분`이라 부르지 않는다. 필요하면 주어를 생략한다.

### 4-2. 전문용어

- **첫 등장 시 원어를 괄호로 병기한다.** 같은 문서에서 두 번째부터는 한국어만 쓴다.
  - `컴파일러(compiler)는 소스 코드를 기계어로 번역한다.`
  - `자료형(data type)`, `반복문(loop)`, `매개변수(parameter)`
- 약어는 풀어 쓴 뒤 약어를 병기한다. `통합 개발 환경(IDE, Integrated Development Environment)`
- 한국어 용어는 교육과정 고시의 표기를 따른다. 고시에 없으면 널리 쓰이는 표기를 택하고 데이터의 `핵심개념`에 등록한다.
- 코드 식별자·키워드는 본문에서 `` `for` ``처럼 인라인 코드로 표기한다. 한글 조사와 붙일 때 `` `for`문 ``처럼 붙여 쓴다.

### 4-3. 문장

- 한 문장에 한 개념. 두 줄을 넘기면 자른다.
- 능동태를 쓴다. `~되어진다`, `~라고 할 수 있다` 같은 군더더기를 뺀다.
- `이것`, `그것`, `해당` 같은 지시어로 앞 문장을 가리키지 않는다. 명사를 다시 쓴다.
- 비유는 개념 도입에만 쓰고, 개념 정의는 정확한 말로 다시 적는다.
- 이모지는 섹션 표지·박스 라벨 정도로 제한하고, **이모지로만 정보를 전달하지 않는다**(스크린 리더).

---

## 5. 커밋 규칙

이 폴더는 아직 git 저장소가 아니다. 첫 작업 전 `git init`과 `.gitignore` 설정이 필요하다.

### 5-1. 메시지 형식

```
<종류>(<범위>): <요약 50자 이내>

<본문 — 왜 바꿨는지. 무엇을 바꿨는지는 diff가 말한다>

관련 성취기준: 프그01-01, 프그01-02
확인필요 해소: content/curriculum.json 3건 → 0건
```

> 커밋 메시지에도 **`프그NN-NN` 코드를 쓴다.** `12프로…` 형태를 적으면 나중에 이력에서
> 근거 없는 코드를 되짚게 된다 (3-3, 6장 3번).

| 종류 | 용도 |
|---|---|
| `content` | 차시 본문·문항 집필·수정 |
| `data` | `curriculum.json`, 성취기준·대조표 데이터 |
| `feat` | 기능 추가 (레이아웃, 실행기, 검색) |
| `fix` | 버그 수정 |
| `docs` | PLAN·CLAUDE·분석 문서 |
| `style` | 스타일·토큰 (동작 변화 없음) |
| `refactor` | 동작 변화 없는 구조 개선 |
| `test` | 검증 스크립트 |
| `chore` | 설정·의존성 |

범위 예: `curriculum`, `u01`, `u01l03`, `runner-py`, `runner-c`, `shell`, `tree`, `toc`, `quiz`, `crosswalk`, `search`

### 5-2. 규칙

- 한 커밋에 한 가지 변경. 데이터 수정과 기능 추가를 섞지 않는다.
- 요약은 명사형·동사형 어미로 끝낸다. `추가`, `수정`, `보완` — 마침표 없음.
- **`content/` 또는 `schemas/`를 건드린 커밋은 `npm run check:curriculum`이 통과한 뒤에만 만든다.**
- `[확인필요]`를 새로 늘리는 커밋은 본문에 이유를 적는다. 줄이는 커밋은 근거(출처·확인일)를 적는다.
- 교과서 PDF, 스캔 이미지, 대용량 바이너리를 커밋하지 않는다. `.gitignore`에 `*.pdf`를 넣는다.
- 커밋과 푸시는 요청받았을 때만 한다.
- 기본 브랜치에서 바로 작업하지 않는다. Phase·단원 단위로 브랜치를 딴다(`data/curriculum-phase1`, `content/u01`).

---

## 6. 절대 하지 말 것

1. **출판사 교과서의 본문·삽화·문항·예제를 옮기지 않는다.** 표현만 바꾼 재작성도 포함한다.
2. **교과서 PDF·스캔 이미지를 저장소나 배포물에 넣지 않는다.**
3. **성취기준 코드·문구·영역명을 추측해서 채우지 않는다.** 코드는 `프그NN-NN`이며 근거는 3-3에 있다. 미확인 항목은 `[확인필요]`다. **`12프로` 형태의 코드를 만들지 않는다** — 근거가 없고 고시 코드로 오인된다.
4. **성취기준 매핑이 없는 차시를 만들지 않는다.**
5. **출판사 단원명으로 우리 목차를 대체하지 않는다.** 병기만 한다.
6. **검증 스크립트가 실패한 상태로 커밋·배포하지 않는다.**
7. **`[확인필요]`가 남은 항목을 `published`로 올리지 않는다.**
8. **학생 개인정보를 수집·전송하지 않는다.** 계정·서버·분석 스크립트를 임의로 넣지 않는다. 저장은 `localStorage`까지다.
9. **코드 실행 엔진 실패가 본문 학습을 막게 하지 않는다.** 엔진은 언제나 선택적 기능이다.
10. **실행해 보지 않은 예제 코드를 싣지 않는다.** 기대 출력을 데이터에 함께 적는다.
11. **필수 자산을 외부 CDN에만 의존하지 않는다.** 폰트는 자체 호스팅한다. 학교망 차단 시 조판이 깨지면 안 된다.
12. **PLAN.md 6장 결정 항목이 확정되지 않은 영역의 코드를 먼저 쓰지 않는다.** 특히 기술 스택(1번), C 실행 방식(6번).
13. **한글 폴더명·한글 URL 경로를 쓰지 않는다.** 경로는 번호로 쓴다(`/units/01/03/`).
    단 **앵커 조각(`#제목`)은 예외다** — Astro가 본문 제목에서 자동 생성하며 한글이 들어간다.
    강제로 ASCII로 바꾸면 Astro가 따로 수집하는 목차 id와 어긋나 목차 링크가 깨진다.
    조각은 서버 경로가 아니고 브라우저가 처리하므로 문제되지 않는다.
    `npm run check:html`이 조각과 `id`의 일치를 전수 확인한다.
14. **`[확인필요]` 대신 다른 표기를 쓰지 않는다.** 검증 스크립트가 이 문자열만 센다.
15. **대규모 리팩터링·스택 변경을 임의로 하지 않는다.** 제안하고 승인을 받는다.

---

## 7. 자주 쓰는 명령

개발 서버 (http://localhost:4321):

```bash
npm run dev
```

빌드 — 검사를 모두 통과해야 산출물이 나온다:

```bash
npm run build
```

`build`는 6단계 게이트다. 하나라도 실패하면 빌드가 서지 않는다.

| 단계 | 명령 | 검사 내용 |
|---|---|---|
| 1 | `check:data` | 성취기준 매핑·커버리지·`[확인필요]`·`총차시` 일치 |
| 2 | `check:content` | 본문 프런트매터 ↔ `curriculum.json` 이중 확인, 문항의 성취기준 매핑, `published` 게이트, 문체 |
| 3 | `check:toc` | 목차 강조 판정 로직 단위 테스트 14건 |
| 4 | `check:examples` | 예제 코드를 실제로 실행해 `expect` 대조 (Python·C) |
| 5 | `astro build` | 45페이지 정적 생성 |
| 6 | `check:html` | 중복 id·깨진 앵커·깨진 내부 링크·`lang`·`h1`·`alt`·JS 없이 읽히는지 · **화면 코드 ↔ MDX 원문 일치** |

`check:content`가 잡는 것 (2단계)

- 파일 경로(`01/03.mdx`)와 프런트매터의 `unit`·`lesson` 불일치
- `title`·`standards`·`langs`·`status`가 `curriculum.json`과 어긋남
- `<QuizItem std="…">`이 없는 코드이거나 그 차시가 담당하지 않는 성취기준
- `status: published`인데 `todo`나 `[확인필요]`가 남음
- 해요체·구어체 종결 (경고. 인용문에서 걸릴 수 있어 사람이 판단한다)
- **프런트매터 값이 따옴표로 시작하는데 그 따옴표로 끝나지 않는 경우**
- **JSX 속성 안의 `\"`** — MDX가 깨진다
- **`<CodeSample>` 코드의 1~3칸 들여쓰기** — 아래 「MDX가 들여쓰기 2칸을 지운다」 참고

큰따옴표 두 항목은 2026-08-07에 실제로 겪은 뒤 추가했다. 둘 다 **큰따옴표 때문에 astro build(5단계)에서 원인을 짚기 어려운 오류로 죽는** 경우다.

| 잘못 적은 것 | 무슨 일이 생기나 |
|---|---|
| `lead: "돌아간다"와 "맞다"는 다르다…` | YAML이 `"돌아간다"` 까지만 값으로 읽고 나머지에서 깨진다 → `bad indentation of a mapping entry` |
| `stem="… \"7\" …"` | JSX 속성에는 백슬래시 이스케이프가 없다. 첫 `\"` 에서 속성이 끝나고 그 뒤를 태그로 읽는다 → `Unexpected character after \`<\`` |

**본문이나 속성에서 큰따옴표가 필요하면 `「 」` 를 쓴다.** 꼭 큰따옴표여야 하면 속성값을 `{`…`}` 로 감싼다.

### ★ MDX가 들여쓰기 2칸을 지운다 (2026-08-07 발견·수정)

MDX는 **여러 줄에 걸친 표현식의 둘째 줄부터 들여쓰기 2칸을 지운다.** JSX를 자연스럽게 들여쓸 수 있게 하려는 동작인데, `<CodeSample>` 안의 코드에도 그대로 적용된다.

| MDX에 적은 것 | 화면에 나온 것 |
|---|---|
| `    with open(…)` (4칸) | `  with open(…)` (2칸) |
| `        f.write(…)` (8칸) | `      f.write(…)` (6칸) |

`max(0, n-2)` 규칙이며 예외가 없다. 그냥 두면 **검증한 코드와 화면에 실은 코드가 달라진다** — `check:examples`는 MDX 원문을 읽어 실행하지만, 학생이 보고 복사하는 것은 2칸이 지워진 쪽이다. 6장 10번에 어긋난다.

- **되돌리기**: `CodeSample.astro`의 `들여쓰기복원()` 이 「1칸 이상 들여쓴 줄에 2칸을 다시 붙인다」.
- **감시**: `check:html`(6단계)이 산출물의 `<pre class="code-sample__code">` 를 MDX 원문과 **글자 단위로 대조**한다. 되돌리기가 어긋나면 배포가 멈춘다.
- **울타리**: 1~3칸 들여쓰기는 되돌릴 수 없어(원래 2칸과 0칸이 모두 0칸이 된다) `check:content`가 오류로 막는다. **예제 코드는 4칸 단위로 적는다.**
- 파이썬은 4칸이 표준(PEP 8)이고 C 예제도 4칸으로 맞췄으므로 실제로 걸릴 일은 없다.

같은 이유로 **`<textarea>` 의 여는 태그와 값은 반드시 한 줄에 붙여 쓴다.** 줄을 나누면 `.astro` 파일의 들여쓰기가 값 앞에 들어가 첫 줄이 `IndentationError` 를 낸다.

### 파일을 만드는 예제는 임시 폴더에서 검증한다

`check:examples`는 예제를 **예제마다 새로 만든 임시 폴더에서** 돌린다(`cwd`). 그렇지 않으면 이어 쓰기(`"a"`) 예제가 앞 실행이 남긴 파일에 덧붙어 `expect`와 어긋나고(2단원 18차시가 실제로 그랬다), 저장소 폴더에 `diary.txt` 같은 파일이 생긴다.

본문 하나만 빠르게 검사:

```bash
npm run check:content
```

코드를 건드리지 않고 데이터·로직만 빠르게 검사:

```bash
npm test
```

`[확인필요]` 잔여 목록만 보기:

```bash
npm run check:todo
```

스키마 예시 파일까지 함께 검증:

```bash
npm run check:all
```

### 배포 — GitHub Pages

- 저장소: https://github.com/trmoo/22hs-programming
- 주소: https://trmoo.github.io/22hs-programming/

`.github/workflows/deploy.yml`이 푸시마다 검사 6단계를 돌리고 통과한 것만 올린다. `dist/`는 커밋하지 않는다.

**하위 경로 배포라 `BASE_PATH`가 필요하다.** 워크플로가 `BASE_PATH=/22hs-programming/`을 준다. 로컬에서 같은 조건으로 확인하려면:

```bash
BASE_PATH=/22hs-programming/ npm run build
```

> Git Bash에서는 MSYS가 `/22hs-programming/`을 윈도우 경로(`C:/Program Files/Git/…`)로 바꿔 버려
> 검사가 엉뚱하게 실패한다. PowerShell을 쓰거나 `MSYS_NO_PATHCONV=1`을 붙인다.

**★ Node 22 이상이 필요하다 (2026-08-07 실제로 겪음).** 워크플로를 Node 20으로 두었더니 빌드가 실패했다. `package.json`의 `engines`도 22로 올려 두었다.

**★ 워크플로는 검사 단계를 하나씩 나눠 돌린다.** `npm run build` 한 덩어리로 두면 어느 검사가 깨졌는지 알 수 없다 — **공개 저장소라도 Actions 로그 열람에는 로그인이 필요해서** 실패 원인을 밖에서 못 본다. 단계 이름만 보고 원인을 좁힐 수 있게 나눠 두었으니 합치지 말 것.

**★ Pages 활성화**는 `configure-pages@v5`의 `enablement: true`로 자동으로 켜졌다. 거부되는 저장소도 있으니(`Resource not accessible by integration`) 그때는 Settings → Pages → Source를 **「GitHub Actions」**로 한 번만 바꾼다. **「Deploy from a branch」를 고르면 안 된다** — `dist/`를 커밋하지 않으므로 흰 화면이 뜬다.

---

### 작업 위치 — 로컬 디스크에 둔다

프로젝트 경로는 `D:\project_AI\22hs-programming` 이다. **OneDrive 안에서 작업하지 않는다.**

2026-08-06에 `D:\OneDrive - 와우고등학교\2. class\[교과서+지도서]\22개정 프로그래밍` 에서
옮겨 왔다. OneDrive가 `node_modules/`를 동기화하려 들면서 다음 문제가 실제로 났다.

- `npm install` 이 패키지 46개를 교체할 때 **1000개가 넘는 파일 삭제 경고**가 떴다
- OneDrive 파일 잠금으로 설치가 실패했다 — `npm warn cleanup EPERM: unlink … esbuild.exe`
- 설치 시간이 로컬 디스크의 7배였다 (48초 → 7초)

`node_modules/`, `dist/`, `.astro/` 는 명령 한 줄로 복원되는 산출물이므로 클라우드에 둘 이유가
없다. 백업이 필요한 것은 소스(`content/`, `src/`, `docs/`, `tools/`)이고, 그것은 git 저장소로
관리한다.
